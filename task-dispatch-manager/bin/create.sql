DROP TABLE pop_task_dispatch_taskinfo;
CREATE TABLE `pop_task_dispatch_taskinfo` (
  `ID` BIGINT(50) NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `SYS_KEY` BIGINT(50) NOT NULL COMMENT '',
  `NAMESPACE` VARCHAR(10) NOT NULL COMMENT 'namespace',
  `BIZ_TYPE` VARCHAR(10) NOT NULL COMMENT '',
  `EXECUTE_TYPE` INT(10) NOT NULL COMMENT '',
  `EXECUTE_TOPIC` VARCHAR(255) NOT NULL COMMENT '',
  `EXECUTE_ID` VARCHAR(50) NOT NULL COMMENT '',
  `EXECUTE_CONTEXT` VARCHAR(255) NOT NULL COMMENT '',
  `CLIENT_TIME` BIGINT(50) NOT NULL COMMENT '',
  `EXECUTE_TIME` BIGINT(50) NOT NULL COMMENT '',
  `STRATEGY` INT(10) DEFAULT 0 COMMENT '',
  `CREATED` DATETIME NOT NULL COMMENT '创建时间',
  `MODIFIED` DATETIME NOT NULL COMMENT '修改时间',
  PRIMARY KEY (`ID`),
  UNIQUE KEY `UNIQ_IDX_SYS_KEY` (`SYS_KEY`),
  KEY (`EXECUTE_TIME`)
) ENGINE=INNODB AUTO_INCREMENT=27812 DEFAULT CHARSET=utf8 COMMENT='重试任务表'